# Server deployment

Use a stable absolute workspace path and keep actual runtime data outside the public Git history. Code changes should be reviewed before updating a live clone. Service names, restart commands, secrets, and backup locations depend on the operator's environment and are intentionally not hard-coded here.
